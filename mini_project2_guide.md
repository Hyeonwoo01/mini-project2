# 미니프로젝트 2 — 웹 크롤링 기반 의사결정용 인터랙티브 스토리텔링 대시보드

**기간:** 5일  
**핵심 기술:** Python · Pandas · 크롤링/Open API · DB(SQL) · Streamlit(Plotly · Seaborn)  
**선행:** 미니프로젝트 1 (공공데이터 기반 지역별 통계 분석)  
**최종 산출물:** 동작하는 Streamlit 대시보드 + 리포트

---

## 1. 미니프로젝트 1과 다른 점

미니프로젝트 1이 **"나는 이 데이터에서 무엇을 발견했는가"** 였다면,  
미니프로젝트 2는 **"내 발견을, 결정을 내려야 하는 사람에게 어떻게 전달할 것인가"** 입니다.

| | 미니프로젝트 1 | 미니프로젝트 2 |
|---|---|---|
| 데이터 확보 | CSV 다운로드 (정적, 1회) | **Open API / 크롤링** (자동화, 재수집 가능) |
| 데이터 성격 | 단면(snapshot) 위주 | **시계열·주기적 갱신** 데이터 포함 |
| 산출물 | 리포트 (읽는 사람이 나) | **대시보드** (쓰는 사람이 타인) |
| 성공 기준 | 분석이 타당한가 | **의사결정에 쓸 수 있는가** |
| 청중 | 나 자신 / 강사 | **경영진 · 공공기관 담당자** |

### 이번 과제의 핵심 전환

> **"차트를 여러 개 만드는 것"이 중요한게 아닙니다.**  
> 대시보드는 **하나의 주장을 하는 화면**입니다. 청중이 대시보드를 보고 **의사결정을 내림에 있어 도움을 받을 수 있어야 합니다.**

미니프로젝트 1에서 세운 분석 방향을 **그대로 이어받는 것을 권장**합니다.  
데이터를 이미 이해하고 있으니, 5일 중 상당 부분을 "전달"과 "구현"에 쓸 수 있습니다.

### 학습 목표

| 영역 | 훈련되는 것 |
|---|---|
| **아키텍처 설계** | 수집·저장·조회를 **분리하고 계약으로 연결**하는 구조 감각 |
| **Open API / 크롤링** | 인증·페이징·재시도·rate limit을 다루는 수집 파이프라인 설계 |
| **데이터 파이프라인** | 수집 → 정제 → 적재를 **반복 실행 가능한 코드**로 만들기 |
| **DB / SQL** | 대시보드가 빠르게 조회할 수 있는 집계 테이블(마트) 설계 |
| **시각화** | 목적에 맞는 차트 선택, 인터랙션 설계 |
| **Streamlit** | 상태 관리, 캐싱, 필터 연동 UI 구현 |
| **스토리텔링** | 데이터를 "그래서 무엇을 해야 하는가"로 번역하기 |

---

## 2. 목표 구조

### 2-1. 기본 목표 (필수 — 합격선)

이번 과제는 **세 개의 독립된 계층을 만들고, 그 사이를 DB로 연결하는 구조**로 구현합니다.
(설계 원칙과 이유는 §4-1에서 자세히 다룹니다 — **구현 전에 반드시 읽으세요**)

```
[1] 주제·청중 정의  →  [2] 계층 간 스키마 확정   ← Day 1~2에 먼저 정한다

   ┌─────────────┐        ┌─────────────┐        ┌─────────────┐
   │  ① 수집기    │  적재   │   ② DB      │  조회   │ ③ 대시보드   │
   │ Collector   │ ─────▶ │  Warehouse  │ ◀───── │  Streamlit  │
   │ API·크롤링   │ (쓰기)  │ 원본 + 마트   │ (읽기)  │  조회 전용   │
   └─────────────┘        └─────────────┘        └─────────────┘
      독립 실행              유일한 접점              읽기만 한다
      (배치/CLI)                                  API 직접 호출 X

                              ↓
                      [3] 스토리라인 구성 + 발표
```

> **핵심 규칙:** 화살표는 **한 방향**입니다.
> 대시보드는 DB만 보고, 수집기는 DB에만 씁니다. **대시보드가 API를 직접 호출하는 순간 이 과제의 설계 목표는 실패**입니다.

**최소 요건 체크**

- [ ] **수집기 · DB · 대시보드가 물리적으로 분리**되어 있다 (별도 디렉터리 / 별도 실행 진입점)
- [ ] **수집기를 실행하지 않아도 대시보드가 단독으로 뜬다** (DB에 데이터가 있다면)
- [ ] **대시보드 코드에 `requests` / 크롤링 / API 키가 등장하지 않는다**
- [ ] **Open API를 통한 데이터 수집이 코드로 구현**되어 있고, 다시 실행하면 이어서 재수집된다
- [ ] **유동인구·방문인구 등 시계열 축 데이터를 1개 이상 확보**했다
- [ ] 수집 데이터가 **2개 소스 이상** (미니프로젝트 1의 CSV 데이터와 결합 가능)
- [ ] **거주인구 등으로 정규화한 파생지표**를 사용한다.
- [ ] 수집 원본 → 정제 → DB 적재가 **분리된 단계**로 구현되어 있다
- [ ] 대시보드가 조회하는 **집계/마트 테이블**이 별도로 존재한다 (원본 전체를 매번 읽지 않음)
- [ ] 서로 다른 목적의 **시각화 2종 이상** (예: 추이 / 구성 / 비교 또는 분포)
- [ ] 대시보드에 metric으로 만든 **핵심 지표(KPI) 영역**이 있다
- [ ] 화면에 **결론 문장**이 있다

### 2-2. 심화 확장 (선택 — 기본 목표 완료 후)

> 기본 목표를 완수한 뒤에 추가적으로 도전해볼 수 있는 과제입니다.  
> 개인 과제이므로 **하나만 골라 제대로 하는 편**이 여러 개를 얕게 다루는 것보다 좋습니다.

- **지도 기반 시각화** — Plotly `scatter_map` / folium. 행정경계 choropleth
- **성능 최적화** — `st.cache_data` 전략 설계, 쿼리 인덱스 튜닝, 응답시간 측정 전후 비교
- **배포** — Streamlit Community Cloud 등에 배포하고 URL 제출

---

## 3. Open API 데이터 제안 (2026년 8월 기준)

> 아래는 **후보**입니다. 미니프로젝트 1의 Base 데이터와 무엇으로 연결되는지를 기준으로 고르세요.

### 3-A. 유동인구·방문인구 시계열 API ⭐

**미니프로젝트 1에서 대부분 "지역별 연령·성별 구조 × 상권" 관점으로 분석했습니다.**  
그 분석의 가장 큰 한계는 **주민등록 인구가 "밤에 자는 사람"만 센다**는 점이었습니다.

> 상권을 움직이는 건 거주인구가 아니라 **그 시간에 그 자리에 있는 사람**입니다.  
> 주민등록 인구 3만 명인 동과 유동인구 30만 명인 동은 완전히 다른 상권입니다.

| MP1에서 할 수 있었던 질문 | MP2에서 가능해지는 질문 |
|---|---|
| 이 동은 20대 인구가 많다 | 이 동은 **평일 낮에** 20대가 몰리고 **주말엔 빠진다** |
| 인구 대비 카페가 많다/적다 | 유동인구가 **늘고 있는데** 점포가 안 따라오는 곳은 어디인가 |
| 고령 인구 비율이 높다 | 고령 방문인구가 **계절마다** 어떻게 달라지는가 |
| 지역 간 격차가 있다 | 그 격차가 **좁혀지는 중인가 벌어지는 중인가** |

| API | 링크 / 검색어 | 무엇을 얻는가 | 조인 키 |
|---|---|---|---|
| **한국관광공사 빅데이터 지역별 방문자수** ⭐ | https://www.data.go.kr/data/15101972/openapi.do | <cite index="92-1">KT(내국인)·SKT(외국인) 이동통신 데이터 기반의 광역·기초지자체별 방문자 수</cite>. **전국을 다루면서 일자별 시계열**을 주는 거의 유일한 공식 API | 시군구 코드 + 일자 |
| **서울 생활인구 (내국인)** ⭐ | https://data.seoul.go.kr/ 검색: "서울 생활인구" | 통신+공공데이터로 추계한 **행정동 × 시간대 × 성별 × 연령대** 인구. <cite index="74-1">일·시각 단위로 생산되며 매일(5일 전 데이터) 갱신</cite> | 행정동코드 + 일자 + 시간 |
| **서울 생활이동** | https://data.seoul.go.kr 검색: "생활이동" | 출발지→도착지 이동량. "이 상권 손님은 어디서 오는가" | 행정동 쌍 + 시간 |
| **지하철·버스 승하차 인원** | https://data.seoul.go.kr 검색: "지하철 역별 승하차"<br>공공데이터포털: "승하차 인원" | 역/정류장 단위 **일·월별 시계열**. 유동인구 대리지표로 가볍고 다루기 쉬움 | 역명·정류장 → 좌표/행정동 매핑 필요 |
| **한국문화관광연구원 관광자원통계서비스** | https://www.data.go.kr/data/15000366/openapi.do | <cite index="87-1">기간·지역·관광지별 내/외국인 방문객 수 조회</cite>. 지점 단위 월별 시계열 | 지역 + 연월 |
| 경기도 유동인구 시군구 단위 집계 | https://www.data.go.kr/data/15128451/fileData.do | 시군구 단위에서 시간대별, 성별 및 연령별로 구분된 유동인구(생활인구)를 집계한 자료 | 시군구 + 시간 + 성별 + 연령대 |
| 시·군 단위 유동인구 API | data.go.kr 검색: "OO시 유동인구" | 일부 기초지자체가 시간대별 유동인구 API 개방 (예: 광명시) | 지자체마다 상이 |

#### 유동인구 데이터를 쓸 때 주의할 점

1. **"유동인구", "생활인구", "방문자수"는 다릅니다.**
   - *유동인구* — 그 지점을 지나간 사람 (통신사 기지국/보행 기준)
   - *생활인구* — 특정 시각에 그 지역에 **존재하는** 인구 (거주자 포함)
   - *방문자수* — <cite index="92-1">일상생활권을 벗어나 관광 등 목적으로 일정시간 머문 사람으로, 방문 목적을 알 수 없어 '관광객'과 동일하게 정의되지 않음</cite>

2. **집계 기준이 달라 단순 합산이 안 되는 경우가 있습니다.**  
   방문자수는 <cite index="92-1">일자별 순방문자 수로 2박 3일 체류 시 3명으로 집계되며, 기초지자체와 광역지자체가 별도 집계 기준을 가져 임의 합산이 불가능</cite>합니다.  
   → **구 단위를 더해서 시 전체를 만들지 마세요.** 시 단위 값을 따로 받는게 좋습니다.

3. **서울 생활인구 API는 조회 가능 기간에 제약이 있습니다.**  
   <cite index="79-1">집계구 단위 생활인구는 Sheet와 OpenAPI가 5일 전 당일 자료만 제공</cite>합니다. 행정동 단위도 기간 조회 폭이 제한적일 수 있습니다.  
   → **대응:** 과거 시계열은 **파일 다운로드**로 받고, 최근 구간만 **API로 추가 수집**하세요.

4. **통신 데이터 기반 추계값입니다.** 실측이 아니라 시장점유율 보정을 거친 추정치이고, 소수점이 나옵니다. **절대값을 신뢰하지 말고 상대 비교·추세로 읽으세요.**

5. **개인정보 보호 마스킹** — 소수 인원 구간은 `*` 등으로 처리되어 옵니다. 이걸 0으로 바꿀지 결측으로 둘지 **판단하고 근거를 남겨두세요.**

6. **코로나 구간(2020~2022)** — 시계열에 이 구간이 들어가면 추세가 왜곡됩니다. 신중하게 포함여부를 선택하세요.

### 3-B. 보조 시계열 축 (유동인구와 함께 쓰면 좋은 것)

| API | 링크 / 검색어 | 무엇을 얻는가 | 조인 키 |
|---|---|---|---|
| **행정안전부 주민등록 인구/세대 API** ⭐ | https://www.data.go.kr/data/15108065/openapi.do (행정동 통반단위)<br>https://www.data.go.kr/data/15108092/openapi.do (도로명별) | 월 단위 **거주인구** 시계열.<br>**유동인구 ÷ 거주인구 = 주간활동 배율** 같은 핵심 파생지표의 분모 | 행정기관코드 + 통계년월 |
| **KOSIS 통계자료 API** | https://kosis.kr/openapi/ | 사업체수·종사자수·고령인구 등 국가통계 시계열 | 지역코드 + 기간 |
| 기상청 종관기상관측(ASOS) 일자료 | data.go.kr 검색: "기상청 지상 일자료" | **유동인구 해석에 날씨요인 적용** "이 주에 사람이 준 게 상권 문제인가 비가 온 건가" | 관측지점 → 시군구 매핑 |
| 한국천문연구원 특일 정보 | data.go.kr 검색: "특일 정보" | 공휴일·명절 정보. 요일·연휴 효과 통제 | 날짜 |
| 국토교통부 아파트 매매/전월세 실거래가 | https://www.data.go.kr/data/15126469/openapi.do | <cite index="45-1">법정동코드 앞 5자리와 계약년월 6자리로 조회</cite>. 지역 경제력 시계열 | 법정동코드 5자리 + 계약년월 |
| 한국부동산원 부동산통계 Open API | https://www.reb.or.kr/r-one/portal/openapi/openApiIntroPage.do | 가격지수·거래량 등 가공 지표 | 지역코드 |

> **거주인구 데이터를 정규화에 적용하세요.**  
> 유동인구를 **거주인구로 나눈 배율**로 만들어 정규화 지표로 만든 뒤 활용하세요.

> ⚠️ **KOSIS 사용 시 주의사항**
> 파라미터(`orgId`, `tblId`, `objL1`, `itmId` 등)가 통계표마다 달라 **처음 호출 성공까지 시간이 걸립니다.** KOSIS 사이트의 URL 생성기로 먼저 동작하는 URL을 만든 뒤 코드로 옮기세요. <cite index="49-1">2026년 기준 분당 호출건수 제한이 적용되고 HTTP 프로토콜 제공이 종료(HTTPS만 사용)</cite>되었으니 요청 간 `sleep`을 반드시 넣으세요. 참고로 통계 소관 부처는 **국가데이터처**로 개편되어 있어, 포털에 따라 기관명이 "통계청" 또는 "국가데이터처"로 혼재 표기됩니다.

### 3-C. 공간·시설 축 API (MP1 Base와 연결되는 축)

| API | 링크 / 검색어 | 무엇을 얻는가 |
|---|---|---|
| **소상공인시장진흥공단 상가업소 정보 조회 서비스** ⭐ | data.go.kr 검색: "소상공인 상가업소 정보 조회" | 반경 상권 / 사각형 영역 / 행정구역 단위 상가 조회. **반경 검색 파라미터(cx, cy, radius)** 제공 |
| **한국관광공사 국문 관광정보 서비스(TourAPI 4.0)** | https://www.data.go.kr/data/15101578/openapi.do | <cite index="64-1">전국의 관광정보와 사진정보를 실시간으로 제공</cite>. 위치기반 조회(`locationBasedList`), 지역기반 조회, **축제/행사 기간 조회** 지원 → 유동인구 급증 구간의 원인 설명에 유용 |
| 국립중앙의료원 전국 병의원 찾기 | data.go.kr 검색: "병의원 정보 조회" | 의료 인프라 위경도 |
| 보건복지부 어린이집·유치원 정보 | data.go.kr 검색: "어린이집 정보" | 보육 인프라 |
| 한국환경공단 에어코리아 대기오염정보 | data.go.kr 검색: "대기오염정보 조회" | 측정소별 시간별 대기질 (**시계열 + 공간 동시 확보**) |
| 도로교통공단 교통사고 정보 개방 | data.go.kr 검색: "교통사고정보" | 사고 다발지역 위경도 + 연도 |

### 3-D. 사업체·경제 축 API

| API | 검색어 | 무엇을 얻는가 |
|---|---|---|
| 공정거래위원회 가맹사업 정보 | "가맹사업" | 브랜드별 가맹점·직영점 수, 매출액, 계약상태 |
| 국세청 사업자등록 상태조회 | "사업자등록 상태조회" | 사업자 상태 검증 |
| 소상공인시장진흥공단 전통시장 정보 | "전통시장" | 시장 위치·점포수 |
| 고용노동부 워크넷 채용정보 | "채용정보" | 지역별 구인 수요 시계열 |

### 3-E. 지자체 자체 포털 (풍부하지만 표준화는 약함)

- **서울 열린데이터광장** — https://data.seoul.go.kr/ (**생활인구·생활이동·상권분석·지하철 승하차** 등 가장 밀도 높은 시계열 데이터들이 존재. **서울을 대상 지역으로 잡는다면 최우선 고려대상**)
- **경기데이터드림** — https://data.gg.go.kr/
- **LOCALDATA(지방행정 인허가)** — https://www.localdata.go.kr/ (**변동분 Open API 제공**. <cite index="38-1">OPEN API는 변동분 자료만 호출 가능하며 전체 자료는 다운로드 페이지를 이용</cite>해야 하므로, **전체분 CSV + 변동분 API 조합**을 연습해볼 수 있습니다.)

---

### 3-F. 주제 아이디어 (그대로 쓰지 말고 변형할 것)

각 아이디어는 **"누가 이 화면을 보고 무엇을 결정하는가"** 가 명시되어야 합니다.  
MP1의 연령·성별 분석에서 **시간축을 추가하면** 자연스럽게 이어지는 형태로 구성했지만 어떤걸 보여줄지는 자유입니다.

| # | 청중 & 결정 | Base(MP1 계승) | + 시계열 API | 대시보드가 답하는 질문 |
|---|---|---|---|---|
| 1 | **프랜차이즈 출점 전략팀** — 다음 분기 출점 후보 3곳 | 상가(상권)정보 + 연령별 인구 | 생활인구 / 방문자수 | 우리 타깃 연령대의 **유동인구가 늘고 있는데** 경쟁 점포는 아직 적은 곳은 |
| 2 | **상권 활성화 담당(지자체)** — 지원사업 대상 상권 선정 | 상권 데이터 | 생활인구 + 거주인구 | **낮에 사람이 빠져나가는 동**과 **모이는 동**은 어디이고, 그 흐름이 바뀌고 있는가 |
| 3 | **관광재단 마케팅팀** — 하반기 집중 홍보 지역·시기 | 관광·숙박 인프라 | 방문자수 + 관광지 입장객 + 기상 | 방문자는 느는데 **체류 인프라가 못 따라가는** 지역과 **비수기 공략 시점**은 |
| 4 | **대중교통 정책 담당** — 증차·배차 조정 구간 | 상권/시설 위치 | 지하철·버스 승하차 시계열 | 수요가 늘어난 구간과 그 시간대는. **거주인구 변화로 설명되는가** |
| 5 | **소상공인 지원기관** — 폐업 위험 상권 조기경보 | LOCALDATA 전체분 | LOCALDATA 변동분 + 유동인구 | **유동인구가 줄기 시작한 뒤 폐업이 늘어나는가** — 선행지표를 찾을 수 있는가 |
| 6 | **고령친화 정책 담당** — 우선 정비 지역 선정 | 병의원·복지시설 | 연령별 생활인구 | 고령 **활동인구**가 많은 시간대·지역과 시설 접근성의 불일치는 |

---

## 4. 구현 가이드

### 4-1. 계층 분리 설계 ⭐

#### 왜 분리하는가

가장 쉬운 구현은 Streamlit 파일 하나에 전부 넣는 것입니다. 그리고 그건 **거의 항상 이렇게 망가집니다.**

| 합쳐서 만들면 | 실제로 벌어지는 일 |
|---|---|
| 대시보드가 열릴 때마다 API 호출 | 화면 새로고침 = API 호출. **하루 트래픽 한도가 금방 소진**되고, 그때부턴 화면이 뜨지 못함 |
| 데이터가 메모리에만 존재 | 앱을 끄면 사라짐. 어제 결과와 오늘 결과를 비교 불가 |
| API가 장애/변경되면 | 대시보드 전체가 뜨지 않음 |

분리하면 이렇게 바뀝니다.

- **실패 격리** — API가 죽어도 대시보드는 어제 데이터로 정상 동작합니다
- **속도** — 대시보드는 이미 집계된 테이블만 읽으므로 즉시 보여집니다
- **작업 순서의 자유** — 스키마만 먼저 정해두면 **수집이 끝나기 전에 대시보드를 만들 수 있습니다.**

> 이 구조는 실무에서 **ETL(수집·정제·적재) ↔ 데이터 웨어하우스 ↔ BI 시각화**로 부르는 구조와 일맥상통합니다.  
> 사용하는 기술만 다르게하여 데이터 파이프라인을 만드는게 목표입니다.

#### 계층별 책임

| | ① 수집기 (Collector) | ② DB (Warehouse) | ③ 대시보드 (App) |
|---|---|---|---|
| **하는 일** | API 호출·크롤링, 정제, DB 적재 | 데이터 보관, 집계 테이블 제공 | 조회, 시각화, 스토리 전달 |
| **실행 방식** | 배치 (CLI에서 수동/스케줄 실행) | 항상 존재 (파일 또는 서버) | 상시 (`streamlit run`) |
| **DB 권한** | **쓰기** | — | **읽기 전용** |
| **가져도 되는 것** | API 키, `requests`, 재시도 로직 | 스키마, 인덱스, 집계 SQL | `SELECT` 쿼리, 차트, 위젯 |
| **가지면 안 되는 것** | 차트 코드, Streamlit 의존성 | 비즈니스 로직 없는 원본 덤프만 있는 상태 | **API 키, `requests`, 크롤링, `INSERT`/`UPDATE`** |

#### DB 스키마를 먼저 정한다

세 계층이 만나는 지점은 **DB 스키마 하나뿐**입니다.

**Day 1~2 사이에 `schema.sql`을 먼저 확정하세요.** 데이터가 한 건도 없어도 상관없습니다.

1. **대시보드 화면을 먼저 상상합니다.** "KPI 4개, 월별 추이 선그래프, 지역 비교 막대"
2. **그 화면을 그리려면 어떤 컬럼이 필요한지 역산합니다.** → 이게 마트 테이블 스키마
3. `seed_dummy.sql`로 **임시 데이터를 INSERT** 해둡니다
4. **수집기를 돌려놓고 대시보드를 작업합니다.**
5. 실제 데이터가 적재되는 순간, 대시보드는 코드 수정 없이 실 데이터로 바뀝니다

> "수집 다 끝나고 대시보드 개발"로 가면 대기시간이 발생하고, 그때부터 만들게 되면 시간이 부족할 수 있습니다.

**스키마 설계 시 고려할 것** (`db/README.md` 권장)

| 항목 | 예시 |
|---|---|
| 테이블 목적 | `mart_region_monthly` — 대시보드 메인 조회용, 지역×월 단위 집계 |
| 컬럼·타입·단위 | `avg_daily_pop REAL` — 일평균 유동인구(명), 추계값이라 소수점. NULL 가능(미집계일) |
| 그레인 | 1행 = 1행정동 1개월 |
| 갱신 주기 | `build_mart.sql` 실행 시 마트 데이터 추가 |
| 지역코드 체계 | **행정동코드** 10자리 문자열 (법정동코드 아님, 앞자리 0 유지) |
| 기준시점 | `base_ym` = 데이터 기준연월 (수집일 아님) |
| 지표 정의 | `activity_ratio` = 일평균 유동인구 ÷ 주민등록 거주인구 |

#### 프로젝트 구조 (권장)

```
project/
├── README.md                    # 개요 · 실행 순서 · 스크린샷
├── .env.example                 # 키 "이름"만 (실제 값은 커밋 금지)
├── requirements.txt
├── commands                     # (추천) 계층별 실행 명령어들 정리
│
├── collector/                   # ───── ① 수집 계층 (Streamlit 의존성 없음)
│   ├── __main__.py              #   진입점: python -m collector
│   ├── config.py                #   지역코드 목록, 수집 기간, 엔드포인트
│   ├── client.py                #   공통 HTTP 호출기 (재시도·딜레이·로깅)
│   ├── sources/
│   │   ├── flow_pop.py          #   유동인구/생활인구 수집
│   │   ├── resident_pop.py      #   주민등록 거주인구 수집
│   │   └── crawl_xxx.py         #   (선택) 크롤링
│   ├── transform.py             #   정제·타입 변환·결측 처리
│   └── loader.py                #   DB 적재 (UPSERT) + 적재 검증
│
├── db/                          # ───── ② 저장 계층 (수집기와 대시보드 연결 매개체)
│   ├── README.md                #   수집기와 대시보드가 공유하는 스키마 문서
│   ├── schema.sql               #   원본 + 마트 테이블 DDL, 인덱스
│   ├── build_mart.sql           #   원본 → 마트 집계 쿼리
│   └── seed_dummy.sql           #   대시보드 선행 개발용 더미 데이터
│
├── app/                         # ───── ③ 대시보드 계층 (읽기 전용)
│   ├── main.py                  #   Streamlit 진입점: streamlit run app/main.py
│   ├── repository.py            #   ⭐ DB 조회 함수만 모음 (SELECT 전용 + 캐싱)
│   ├── charts.py                #   DataFrame → Figure 변환
│   └── components.py            #   KPI 카드, 사이드바 필터
│
├── data/                        #   .gitignore에서 제외 (로컬에서만 관리)
│   ├── raw/2026-08-18/          #   API 응답 원본 (예시)
│   └── logs/                    #   수집 로그
└── report/
    └── report.md
```

**구조의 포인트**

- `collector/`는 `app/`을, `app/`은 `collector/`를 **절대 import 하지 않습니다**
- `app/repository.py`가 **DB 통신, 데이터 조회를 위한 파일**입니다.
- `app/charts.py`는 DataFrame을 받아 차트를 그리는 파일입니다.

#### 실행 흐름 (commands 예시)

```makefile
init:       # DB 초기화
	mysql -u analyzer -p < db/schema.sql

seed:       # 더미 데이터 주입 (대시보드 선행 개발용)
	mysql -u analyzer -p < db/seed_dummy.sql

collect:    # ① 수집 계층 실행
	python -m collector

mart:       # 마트 재생성
	mysql -u analyzer -p < db/build_mart.sql

dash:       # ③ 대시보드 실행
	streamlit run app/main.py
```

#### 성능을 위한 설계 패턴

| 안티패턴 | 문제점 | 성능 향상 방법 |
|---|---|---|
| Streamlit에서 크롤링 코드 실행 | 계층 붕괴. 트래픽 한도 소진, 장애 발생 시 대시보드 사용불가 | 수집기로 옮기고 DB를 경유 |
| `@st.cache_data`로 API 호출을 감쌈 | 캐시는 성능 도구지 저장소가 아님. 앱 재시작 시 전부 날아감 | DB에 적재 |
| 대시보드에서 `pd.read_csv("data/raw/...")` | DB를 건너뜀. 파일 종속 | 마트 테이블 조회로 변경 |
| 대시보드에서 무거운 `groupby`·`merge` | 집계는 사전에 끝나 있어야 함 | `build_mart.sql`로 이동 |

### 4-2. Open API 수집

> **① 수집 계층(`collector/`)** 에서 구현

**재시도·대기·로깅이 들어간 공통 호출기를 하나 만든다**

```python
import time, requests
from requests.adapters import HTTPAdapter
from urllib3.util.retry import Retry

def make_session(retries=3, backoff=0.5):
    s = requests.Session()
    retry = Retry(total=retries, backoff_factor=backoff,
                  status_forcelist=[429, 500, 502, 503, 504])
    s.mount("https://", HTTPAdapter(max_retries=retry))
    return s

def fetch(session, url, params, sleep=0.3):
    r = session.get(url, params=params, timeout=10)
    r.raise_for_status()
    time.sleep(sleep) # rate limit 고려
    return r.json()
```

**페이징을 처리한다** — `pageNo`, `numOfRows`, `totalCount`를 읽어 끝까지 순회
**원본 응답을 그대로 저장한다** — `data/raw/2026-08-18/apt_11110_202601.json` 형태. 바로 DB에 적재하는것도 가능
**수집 로그를 남긴다** — 어떤 파라미터로 몇 건을 언제 받았는지. 실패 건도 기록

### 4-3. DB 설계 — 원본 테이블과 마트 테이블을 분리

대시보드가 매번 수십만 행을 읽으면 느립니다. **미리 집계해둔 마트 테이블**을 만드세요.

유동인구 데이터는 **행 수가 엄청나게 많습니다.** 서울 행정동 425개 × 24시간 × 성별 2 × 연령대 14 × 365일이면 이론상 5천만 행이 넘습니다. **원본을 그대로 대시보드에서 불러오면 오래걸립니다.**

```sql
-- ───── 원본 계층 (수집 그대로 + 최소 정제, 긴 형태로 보관)
CREATE TABLE flow_pop_raw (
    region_code   TEXT,        -- 행정동코드 (자릿수 통일)
    base_date     TEXT,        -- 'YYYY-MM-DD'
    hour          INTEGER,     -- 0~23
    gender        TEXT,        -- 'M' / 'F'
    age_band      TEXT,        -- '20s', '30s' ...  ← 원본 세분류를 묶은 값
    pop_cnt       REAL,        -- 추계값이라 소수점
    collected_at  TEXT
);
CREATE INDEX idx_flow_region_date ON flow_pop_raw(region_code, base_date);

-- ───── 마트 계층 (대시보드 조회 전용, 미리 집계)
-- ① 메인: 지역 × 월 요약
CREATE TABLE mart_region_monthly (
    region_code      TEXT,
    base_ym          TEXT,       -- 'YYYY-MM'
    avg_daily_pop    REAL,       -- 일평균 유동인구
    peak_hour        INTEGER,    -- 최다 시간대
    weekday_pop      REAL,       -- 평일 평균
    weekend_pop      REAL,       -- 주말 평균
    resident_pop     INTEGER,    -- 거주인구 API 조인
    activity_ratio   REAL,       -- 유동 ÷ 거주 = 주간활동 배율
    store_count      INTEGER,    -- 상권 데이터 조인
    pop_per_store    REAL,       -- 점포당 유동인구
    mom_change       REAL,       -- 전월 대비 증감률
    PRIMARY KEY (region_code, base_ym)
);

-- ② 드릴다운: 지역 × 시간대 × 연령 프로파일 (월 단위로 압축)
CREATE TABLE mart_hourly_profile (
    region_code   TEXT,
    base_ym       TEXT,
    hour          INTEGER,
    age_band      TEXT,
    avg_pop       REAL,         -- 시간대별 유동인구
    PRIMARY KEY (region_code, base_ym, hour, age_band)
);
```

**마트 설계 원칙**

- **차트 하나 = 마트 하나**로 생각하면 설계가 쉬워집니다. 추이 차트용, 시간대 프로파일용, 지역 비교용
- **집계 단위를 낮추세요.** 일별 원본 → 월별 마트. 시간대는 유지하되 날짜는 평균으로 압축
- 마트 생성은 `build_mart.sql`대신 python 스크립트로 작성해도 됩니다.
- **전월 대비 증감률, 이동평균**도 계산해보세요. — 시계열 데이터에서 자주 쓰이는 기법입니다.

### 4-4. Streamlit 구현 포인트

**① 캐싱 활용**

```python
import streamlit as st, pandas as pd, sqlite3

@st.cache_data(ttl=3600)
def load_mart(region_codes: tuple, ym_from: str, ym_to: str) -> pd.DataFrame:
    with sqlite3.connect("data/warehouse.db") as con:
        q = """
        SELECT * FROM mart_region_monthly
        WHERE region_code IN ({}) AND base_ym BETWEEN ? AND ?
        """.format(",".join("?" * len(region_codes)))
        return pd.read_sql(q, con, params=[*region_codes, ym_from, ym_to])
```

> 캐시 키가 바뀌면 캐시가 새로 고쳐집니다. 리스트 대신 tuple을 써야되는 이유입니다.

**② 레이아웃 기본형**

```python
st.set_page_config(page_title="...", layout="wide")

with st.sidebar:                       # 필터는 사이드바에 모은다
    sido = st.selectbox("시도", ...)
    period = st.slider("기간", ...)

st.title("한 줄 주장 (예: 'A권역 3개 동은 수요 대비 공급이 부족합니다')")

c1, c2, c3, c4 = st.columns(4)         # KPI 영역
c1.metric("거래건수", f"{n:,}", delta=f"{d:+.1%}")
...

tab1, tab2, tab3 = st.tabs(["추이", "지역 비교", "상세"])
```

- `st.metric`의 `delta`는 **비교 기준이 있을 때만** 사용
- 데이터가 없을 때에는 **에러 대신 안내 메시지**를 띄우세요.
- `st.session_state`로 상태 유지, `st.download_button`으로 결과물을 CSV로 제공하는것도 고려해보세요.

**③ 차트 선택 — 목적에 맞게**

| 목적 | 적절 | 피할 것 |
|---|---|---|
| 시간 추이 | 선그래프, 면적그래프 | 시간축을 파이차트로 |
| **시간대 × 요일 패턴** | **히트맵** (24시간 × 7요일) — 유동인구 대시보드의 킬러 차트 | 24개 막대를 나열 |
| **연령대 구성 변화** | 누적 면적그래프(100% 기준) | 연령대별 선 14개를 한 차트에 |
| 구성비 | 누적 막대, 트리맵 | 항목 7개 넘는 파이차트 |
| 지역 비교 | 정렬된 가로 막대 | 정렬 안 된 세로 막대 |
| 두 변수 관계 | 산점도(+추세선) | 이중 축 남용 |
| 분포 | 히스토그램, 박스플롯 | 평균 하나로 퉁치기 |
| 공간 | 지도(choropleth/scatter) | 지역명 텍스트 나열 |

### 4-6. 스토리텔링 구성

경영진이 **데이터를 자연스럽게 읽을 수 있도록 화면을 구성하세요.**

**화면 흐름 (SCQA 구조)**

```
[상단]  결론 한 줄            "B구 3개 동에 우선 출점을 권고합니다"
   ↓
[KPI]   핵심 숫자 3~5개        근거가 되는 지표 (전기 대비 변화 포함)
   ↓
[Tab1]  상황(Situation)        전체 시장은 어떻게 움직여 왔는가 — 추이
   ↓
[Tab2]  문제(Complication)     그런데 지역별로 격차가 있다 — 비교
   ↓
[Tab3]  근거(Answer)           그래서 이 지역이다 — 상세·근거 데이터
   ↓
[하단]  한계와 가정            데이터 기준시점, 검증 못 한 것
```

**대시보드 문구 체크**
- 제목이 "매출 현황"이 아니라 **"3분기 매출은 수도권에서만 성장했습니다"** 처럼 주장인가
- 각 차트에 **"이 차트는 무엇을 말하는지"** 한 줄 캡션이 있는가
- 숫자를 그대로 두지 않고 **"어떤 기준에서 높은/낮은가"** 를 밝히는가
- 데이터의 한계를 화면에 명시해둘 것

---

## 5. AI 활용 가이드

미니프로젝트 1의 원칙을 **그대로 유지**합니다.

> **분석의 방향과 결과 해석은 본인의 생각이어야 하고, AI는 그 생각을 확장하거나 표현을 돕는 데 쓴다.**

### 권장 ✅

- **도메인 지식 확장** — 청중(경영진/공공기관)이 어떤 관점에서 보는지 배경 이해
- **가설·관점 확장** — 내가 정한 주 방향에 대해 놓친 관점 점검 (단, 이 단계에서 코드 생성은 하지 않기)
- **API 명세 해석, 에러 원인 파악, 라이브러리 사용법** — 도구 사용 관련 질문
- **Streamlit/Plotly 문법 질문** — "이 레이아웃은 어떻게 만드나" 수준
- **리포트·발표자료 초안 작성** — 본인이 정리한 내용의 구조화·문장화

### 비권장 ❌

- **분석 결과에 대한 생성형 AI 요약** — 결과를 본인의 표현으로 해석하는 과정 자체가 학습 포인트입니다. 그 훈련이 된 상태에서 쓰는 것과 아닌 상태에서 쓰는 것은 결과물이 비슷해 보여도 남는 것이 완전히 다릅니다
- **주 분석 방향 / 대시보드 스토리라인을 AI에게 위임**
- **대시보드 코드 전체를 생성해 붙여넣기** — 설명하지 못하는 코드는 제출물로 인정되지 않습니다

### 정리

```
분석 방향 · 스토리라인   ← 본인의 생각 (AI는 확장 보조까지)
결과 해석 · 결론 문장    ← 본인의 표현 (AI 요약 X)
구현 보조 · 문서 초안    ← AI 활용 O, 단 본인 검토 필수
```

---

## 6. 5일 타임라인

**계층을 오전/오후 단위로 나눠** 작업하고, 계층을 분리하여 "수집이 끝나야 대시보드를 시작"하지 않는 상황을 가정합니다.

| Day | 오전 | 오후 | 종료 시점 체크포인트 |
|---|---|---|---|
| **Day 1**<br>기획 & 스키마 | 주제·청중 확정, "누가 무엇을 결정하는가" 한 문장<br>MP1 결과 재검토 | **API 신청(가장 먼저 — 승인 지연 위험)**<br>명세 확인, **호출 1건 성공**<br>대시보드 화면 스케치 → 필요 지표 역산 | 🚩 **API 응답을 받아봤다 + 스키마 초안이 있다** |
| **Day 2**<br>스키마 확정 → 수집기 | **`schema.sql` 확정 + `seed_dummy.sql` 작성**<br>DB 초기화 | 수집기 구현 (페이징·재시도·로깅), raw 저장 시작 | 🚩 **더미 DB가 있고, 수집기가 일부 데이터를 받아왔다** |
| **Day 3**<br>대시보드 골격 (더미로) | **더미 DB만 보고** Streamlit 골격 + 사이드바 필터 + KPI 영역 | 차트 3종 구현. **이 시점에 대시보드는 사실상 완성** | 🚩 **더미 데이터로 대시보드가 완성 형태로 돈다** |
| **Day 4**<br>실 데이터 채우기 | 전체 범위 수집 완료, 정제·적재 | **마트 SQL 작성·검증 → 더미를 실 데이터로 교체**<br>깨진 부분 수정, 필터 전면 점검 | 🚩 **코드 수정을 최소로하여 실 데이터로 교체** |
| **Day 5**<br>스토리 | 결과 해석, 스토리라인 재구성, **결론 문장 작성**, 문구 다듬기 | §4-1 **분리 검증 3종 테스트**, 리포트 작성 | 🚩 **처음 보는 사람이 5분 만에 결론을 이해할 수 있도록** |

> **수집기를 터미널을 분리하여 돌려두기**  
> Day 3~4에 수집 스크립트를 터미널 하나에서 돌려두고, 다른 터미널에서 대시보드를 실행하세요.

### 🚩 위험 신호와 대응

| 상황 | 대응 |
|---|---|
| Day 1에 API 승인이 안 났다 | 즉시 대체 API 신청 + **파일데이터(CSV)로 임시 진행**. 승인을 기다리는 동안에도 스키마·더미·대시보드는 만들 수 있음 |
| 수집이 계속 안 끝난다 | **범위를 줄인다.** 전국 → 1개 시도, 5년 → 2년. 넓이보다 완결이 중요 |
| 데이터가 안 붙는다 | 조인 레벨을 올린다. 행정동 → 시군구 단위로 집계 후 조인 |
| 대시보드가 느리다 | 마트 테이블 재설계 + 캐싱. 원본을 직접 읽고 있지 않은지 확인 |
| 실 데이터로 바꾸니 다 깨진다 | 더미가 실제 데이터 형태와 달랐던 것 (타입·NULL·값 범위). **더미를 실제에 가깝게 만드는 것이 예방책** |

> **혼자 하는 과제에서 가장 흔한 실패:** 하나의 계층을 완벽하게 만들려다 나머지에 손도 못 대기.  
> 수집기를 예쁘게 리팩터링하는 것보다, **세 계층이 일단 다 이어지는 상태를 Day 4에 만드는 것**이 훨씬 중요합니다.  
> 각 계층은 "일단 돌아가는" 수준으로 먼저 이어붙이고, 남는 시간에 다듬으세요.

---

## 7. 제출물

1. **코드 저장소** (github) — `README.md`에 시각화 대시보드 예시 포함
2. **동작하는 Streamlit 앱** — 로컬 실행 가능 또는 배포 URL
3. **리포트** (아래 목차)

### 리포트 목차 (권장)

1. **대시보드가 답하는 질문** — 청중은 누구이고 어떤 결정을 내리는가
2. **데이터 수집** — 사용 API 목록, 수집 범위·기간·건수, 수집 시 겪은 문제와 해결
3. **아키텍처** — 수집기/DB/대시보드 계층 구조도, 스키마 설계와 그렇게 나눈 이유, 원본/마트 분리 근거, 분리 검증 테스트 결과
4. **대시보드 구성** — 화면 흐름과 스토리라인 설계 의도, 차트 선택 근거
5. **핵심 인사이트** — 본인의 언어로. 예상과 달랐던 부분 포함
6. **한계와 다음 단계**
7. **(선택) 심화 확장 결과** — 위치는 예시입니다. 적절하다면 리포트 중간에 해당 내용을 포함하세요.
8. **AI 활용 기록**

---

## 8. 평가 기준

| 항목 | 배점 | 세부 |
|---|:--:|---|
| **아키텍처 — 계층 분리** | 20 | 수집기·DB·대시보드가 독립적인가. **의존 방향이 단방향인가.** 대시보드가 읽기 전용인가. §4-1 **검증 3종 테스트 통과** |
| **데이터 수집 파이프라인** | 20 | Open API 수집이 코드로 구현되고 **재실행 가능한가**. 페이징·에러·rate limit 처리. 키 관리가 안전한가 |
| **DB 설계 & 계약** | 15 | 원본/마트 분리, 마트 설계 의도, 조인 검증, 스키마를 계약으로 문서화했는가, SQL 활용 수준 |
| **대시보드 구현 완성도** | 15 | 필터가 전 차트에 일관 반영되는가. 빈 데이터·에러 처리. 응답 속도. 차트 선택이 목적에 맞는가 |
| **스토리텔링 & 논리 정합성** | 20 | **결론이 명시되어 있는가.** 화면 흐름이 주장을 뒷받침하는가. **논리 비약·모순이 없는가.** 상관을 인과로 말하지 않는가 |
| **설명 가능성** | 10 | 데모가 매끄러운가. 코드·결론에 대한 질문에 답할 수 있는가 |
| **(가산) 심화 확장** | +10 | 지도 시각화 / 배포 / 성능 최적화. **기본 목표 완수 시에만 반영** |

**감점 요인**

- API 키가 코드나 저장소에 노출됨 (**중대 감점**)
- **대시보드가 API를 직접 호출함** (계층 붕괴 — **중대 감점**)
- 수집이 수동 작업(브라우저 다운로드)에 의존해 재현 불가
- 대시보드가 CSV를 직접 읽고 DB를 거치지 않음
- 수집기와 대시보드가 서로를 import 함 / 한 파일에 혼재
- 대시보드에서 DB에 쓰기(`INSERT`/`UPDATE`)를 수행함
- 차트만 있고 결론·주장이 없음
- 크롤링 시 딜레이 없이 대량 요청
- 설명하지 못하는 코드·결론

---

## 9. 제출 전 최종 체크리스트

**아키텍처**
- [ ] `collector/` · `db/` · `app/` 이 분리되어 있다
- [ ] 두 계층이 서로를 import 하지 않는다
- [ ] 대시보드 코드에 `requests`·크롤링·API 키가 **없다**
- [ ] `db/README.md`에 테이블 목적·입도·단위·기준시점이 적혀 있다
- [ ] `schema.sql` + `seed_dummy.sql`만으로 화면이 그려진다

**수집**
- [ ] `.env`가 `.gitignore`에 있고, 저장소에 키가 없다
- [ ] 수집 코드를 다시 실행하면 동일하게 동작한다
- [ ] 페이징·재시도·요청 간 딜레이가 구현되어 있다
- [ ] raw 데이터를 수집일자와 함께 보관했다

**DB**
- [ ] `schema.sql`과 마트 생성 SQL이 저장소에 있다
- [ ] 마트 테이블이 존재하고 대시보드는 마트를 조회한다
- [ ] 조인 후 행 수 변화를 확인했다
- [ ] 조회 컬럼에 인덱스가 있다

**대시보드**
- [ ] 빈 결과에서 에러 대신 안내가 뜬다
- [ ] 캐싱이 적용되어 있고 필터 변경 시 체감 지연이 없다
- [ ] KPI 영역이 있고, delta에는 비교 기준이 명시되어 있다
- [ ] 상단에 **결론 문장**이 있다
- [ ] 데이터 기준시점과 출처가 화면에 표기되어 있다

**리포트**
- [ ] 청중과 의사결정이 한 문장으로 정의되어 있다
- [ ] **사용한 인구 개념(유동인구/생활인구/방문자수)을 정의하고 그 한계를 명시**했다
- [ ] 결측을 0으로 채우지 않았고, 처리 방식에 근거가 있다
- [ ] 예상과 달랐던 결과를 숨기지 않았다
- [ ] 상관과 인과를 구분해서 서술했다
- [ ] AI 활용 기록을 남겼다
- [ ] **모든 코드와 결론을 내가 설명할 수 있다**

---

## 10. 참고 링크

- 공공데이터포털 — https://www.data.go.kr/
- 서울 열린데이터광장 (생활인구·생활이동) — https://data.seoul.go.kr/
- 한국관광 데이터랩 (방문자수 해석 유의사항) — https://datalab.visitkorea.or.kr/
- 관광지식정보시스템 (관광지점 입장객) — https://know.tour.go.kr/
- KOSIS 공유서비스(OpenAPI) — https://kosis.kr/openapi/
- 한국부동산원 부동산통계 OpenAPI — https://www.reb.or.kr/r-one/portal/openapi/openApiIntroPage.do
- TourAPI(관광데이터 Hub) — https://api.visitkorea.or.kr/
- LOCALDATA API 이용가이드 — https://www.localdata.go.kr/devcenter/apiGuide.do
- Streamlit 공식 문서 — https://docs.streamlit.io/
- Plotly Python — https://plotly.com/python/

---

### 마지막으로

이 과제에서 중요한 것은 **화면이 화려한 대시보드**가 아닙니다.

좋은 대시보드는

> **이 화면을 처음 보는 사람이, 5분 안에 "그래서 무엇을 해야 하는지"를 말할 수 있는가.**

차트 개수를 늘리는 데 쓸 시간이 있다면, **결론 한 줄을 다듬는 데** 쓰세요.  
그리고 그 결론은 — AI가 아니라 — 여러분이 데이터를 보고 내린 판단이어야 합니다.